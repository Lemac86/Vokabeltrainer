import { ref } from "vue";

export const endeTest = ref(false);
export const questionsAsked = ref<number>(0);
export const correctAnswersPerTestround = ref<number>(0);
export const startTest = ref(false);
